/**
 * Timer component - now delegates to ScoreCountdown for countdown scoring system
 * Kept for backwards compatibility with existing imports
 */
export { ScoreCountdown as Timer } from './ScoreCountdown'
